[git_status]
conflicted = "Conflict💀"
ahead = "ahead🏎💨"
behind = "behind😰"
diverged = "diverged😵"
untracked = "🤷‍"
stashed = "📦"
modified = "📝"
staged = "🗂️"
renamed = "✍️"
deleted = "🗑"

[kubernetes]
format = 'on [⛵ ($user)|($cluster)|$context\($namespace\)](dimmed 213) '
disabled = false
detect_files = ['k8s']

[gcloud]
disabled = true
